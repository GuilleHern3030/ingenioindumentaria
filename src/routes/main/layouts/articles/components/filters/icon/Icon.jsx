import styles from './Icon.module.css'

export default () => {
    return <div className={ styles.icon }>
        <span className={ styles.span }></span>
        <span className={ styles.span }></span>
        <span className={ styles.span }></span>
    </div>
}